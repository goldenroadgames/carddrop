// APIKeys.swift
// Add this file to .gitignore so your keys are never committed.

struct APIKeys {
    /// OpenAI API key — used only for content moderation (free tier).
    static let openAI = Secrets.openAIKey
}
